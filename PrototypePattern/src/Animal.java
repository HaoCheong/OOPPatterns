public interface Animal{

    //Animal Interface
    //Base for all animals, this may also be a bit unnecessary, a clone function can be written as is

    public String getName();
    public double getWeight();
}

